<views:MvxStorePage
    x:Class="HaveIBeenPwned.Mobile.WinRT.Views.FirstView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="using:Cirrious.MvvmCross.WindowsStore.Views"
    mc:Ignorable="d">

    <Grid Background="{StaticResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <TextBox Grid.Row="0" Text="{Binding SearchText, Mode=TwoWay}" />
        <Button Grid.Row="1" Content="Search" Command="{Binding SearchCommand}" />
        <ListBox Grid.Row="2" ItemsSource="{Binding Breaches}" DisplayMemberPath="Title"></ListBox>
    </Grid>
</views:MvxStorePage>